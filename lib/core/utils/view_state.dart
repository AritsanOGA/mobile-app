enum EmployerLoginState { idle, loading, success, failure }

enum JobSeekerLoginState { idle, loading, success, failure }

enum EmployerVerifyCodeState { idle, loading, success, failure }

enum JobSeekerVerifyCodeState { idle, loading, success, failure }

enum EmployerSignUpState { idle, loading, success, failure }

enum JobSeekerSignUpState { idle, loading, success, failure }

enum PostJobState { idle, loading, success, failure }

enum ApplyForJobState { idle, loading, success, failure }

enum GetAllJobState { idle, loading, success, failure }

enum GetFeaturedCandidateState { idle, loading, success, failure }

enum GetFeaturedJobState { idle, loading, success, failure }

enum GetEmployerJobState { idle, loading, success, failure }

enum GetJobSeekerJobState { idle, loading, success, failure }

enum GetCategoryState { idle, loading, success, failure }

enum GetSkillState { idle, loading, success, failure }

enum GetCountryState { idle, loading, success, failure }

enum GetStateState { idle, loading, success, failure }

enum GetJobSeekerProfileState { idle, loading, success, failure }

enum GetEmployerProfileState { idle, loading, success, failure }

enum UpdateEmployerProfileState { idle, loading, success, failure }

enum UpdateJobSeekerProfileState { idle, loading, success, failure }

enum UpdatePasswordState { idle, loading, success, failure }

enum ChangePasswordState { idle, loading, success, failure }

enum ForgotPasswordState { idle, loading, success, failure }

enum AcceptCandidateState { idle, loading, success, failure }

enum RejectCandidateState { idle, loading, success, failure }

enum PostInvoiceState { idle, loading, success, failure }

enum GetInvoiceState { idle, loading, success, failure }

enum GetAllInvoiceState { idle, loading, success, failure }

enum GetAllPaymentState { idle, loading, success, failure }

enum TransferPaymentState { idle, loading, success, failure }

enum CardPaymentState { idle, loading, success, failure }

enum RejectCandidateWithoutInterviewState { idle, loading, success, failure }

enum GetAssignedCandidateState { idle, loading, success, failure }

enum GetCandidateProfileState { idle, loading, success, failure }

enum GetCandidateSkillState { idle, loading, success, failure }

enum FlutterWavePaymentState { idle, loading, success, failure }

enum SearchJobState { idle, loading, success, failure }

enum NoOfCandidateState { idle, loading, success, failure }

enum SearchJobDetailState { idle, loading, success, failure }

enum JobSeekerNotificationState { idle, loading, success, failure }

enum ActvitiesState { idle, loading, success, failure }
