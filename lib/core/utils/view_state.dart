enum ViewState { idle, loading, success, failure }

enum SuccessType {
  none,
  jsRegistration,
  employerRegistration,
  jsLogin,
  employerLogin,
  postJob,
  applyForJob,
  profileUpdate,
  // Add other success types as needed
}
